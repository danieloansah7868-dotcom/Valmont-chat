{
  "name": "vchat",
  "version": "1.0.0",
  "description": "Vchat — secure staged real-time messaging and calling PWA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check lib/http-auth.js && node --check lib/messenger.js && node --check lib/messenger-store.js && node --check lib/runtime-config.js && node --check lib/sms.js && node --check public/app.js && node --check public/sw.js && node --check scripts/backup-local.js && node --check scripts/restore-local.js && node --check scripts/check-runtime-config.js",
    "pilot": "docker compose -f compose.pilot.yml up --build"
  },
  "keywords": [
    "chat",
    "messaging",
    "real-time",
    "socketio"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.2.2",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "multer": "^2.2.0",
    "socket.io": "^4.7.5",
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
